# SentinelAI API

FastAPI service for SentinelAI. See the repository root README for setup and commands.

The validated incident contracts live in `sentinel_api.domain`. The deterministic
fixture generator and packaged golden incident are documented in
[`src/sentinel_api/data/README.md`](src/sentinel_api/data/README.md).
